pip install Flask
pip install python-dotenv
pip install PyPDF2
pip install langchain
pip install langchain-community
pip install langchain-google-genai
pip install numpy==1.21.6  # Ensure it's version 1.21.6 to satisfy dependencies
pip install orjson
pip install langsmith
pip install pytest
